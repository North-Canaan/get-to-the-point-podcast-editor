Vendored browser audio runtime:

- `@ffmpeg/ffmpeg` 0.12.15 (`dist/esm` JavaScript files)
- `@ffmpeg/core` 0.12.10 (`dist/esm/ffmpeg-core.js` and `ffmpeg-core.wasm`)

The files were downloaded from the version-pinned Unpkg package URLs. Keep the
JavaScript wrapper and core versions synchronized when updating this runtime.
